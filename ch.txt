BOOKSHELF CMS — CHAPTER 1 IMPLEMENTATION ROADMAP
Mirrors: "Django 5 by Example", Chapter 1 — Building a Blog Application
Scope rule: every task below maps 1:1 to a section in Chapter 1. Nothing from
Chapter 2+ (pagination, class-based views, forms, comments, tagging, search,
canonical URLs) is included on purpose — see OUT OF SCOPE at the end.

============================================================
0. PROJECT / DOMAIN MAP
============================================================
Django project name   : bookshelf         (book uses: mysite)
Django app name       : catalog           (book uses: blog)
Core model            : Book              (book uses: Post)
Custom manager        : PublishedManager  -> Book.published
Views                 : book_list, book_detail
URL namespace          : catalog
Superuser role         : cataloger/admin who manages BookShelf entries

Target end-state directory tree (Ref: "Creating your first project",
"Creating an application"):

bookshelf/
    manage.py
    bookshelf/
        __init__.py
        asgi.py
        settings.py
        urls.py
        wsgi.py
    catalog/
        __init__.py
        admin.py
        apps.py
        migrations/
            __init__.py
            0001_initial.py
        models.py
        tests.py
        views.py
        urls.py
        templates/
            catalog/
                base.html
                book/
                    list.html
                    detail.html

============================================================
PHASE 0 — ENVIRONMENT & TOOLING
============================================================
Book ref: "Installing Python", "Creating a Python virtual environment",
          "Installing Django" / "Installing Django with pip"

Tasks:
[ ] Confirm Python 3.12 with `python3 --version`
[ ] Create venv: `python -m venv bookshelf_env`
[ ] Activate it (source bookshelf_env/bin/activate, or .\bookshelf_env\Scripts\activate)
[ ] `python -m pip install Django~=5.2`
[ ] Verify: `python -m django --version` -> starts with 5.2

Deliverable: requirements.txt containing `Django~=5.2`
Definition of done: shell prompt shows (bookshelf_env), django --version passes.

============================================================
PHASE 1 — PROJECT BOOTSTRAP
============================================================
Book ref: "Creating your first project", "Applying initial database
          migrations", "Running the development server", "Project settings",
          "Projects and applications"

Tasks:
[ ] `django-admin startproject bookshelf`
[ ] Inspect generated files against the book's manage.py / settings.py /
    urls.py / asgi.py / wsgi.py breakdown
[ ] `cd bookshelf && python manage.py migrate` — apply default
    contenttypes/auth/admin/sessions migrations
[ ] `python manage.py runserver` — confirm the default "it worked" page at
    http://127.0.0.1:8000/
[ ] Read settings.py and annotate (as code comments) what DEBUG,
    ALLOWED_HOSTS, INSTALLED_APPS, MIDDLEWARE, ROOT_URLCONF, DATABASES,
    LANGUAGE_CODE, USE_TZ each control, using bookshelf as the example

Deliverable: running dev server + annotated settings.py
Definition of done: `python manage.py migrate` ends clean, default page loads.

============================================================
PHASE 2 — APP CREATION & DATA MODELING
============================================================
Book ref: "Creating an application", "Creating the blog data models" and
          all its subsections

Tasks:
[ ] `python manage.py startapp catalog`
[ ] Inspect generated admin.py / apps.py / migrations/ / models.py /
    tests.py / views.py

Build the Book model incrementally, exactly following the chapter's build
order (do not skip straight to the final version — each sub-step is its own
checkpoint):

  a) Base fields (Ref: "Creating the Post model")
     title    -> CharField(max_length=250)
     slug     -> SlugField(max_length=250)
     synopsis -> TextField()      [maps to Post.body]
     __str__  -> return self.title

  b) Datetime fields (Ref: "Adding datetime fields")
     publish  -> DateTimeField(default=timezone.now)
                 [the date/time this catalog entry becomes publicly
                 visible — an editorial/CMS date, not the book's real
                 print date]
     created  -> DateTimeField(auto_now_add=True)
     updated  -> DateTimeField(auto_now=True)
     (optional aside, per the book: try db_default=Now() on `publish`
     instead of default=timezone.now to see the DB-computed default)

  c) Default sort order (Ref: "Defining a default sort order")
     class Meta: ordering = ['-publish']

  d) Database index (Ref: "Adding a database index")
     class Meta: indexes = [models.Index(fields=['-publish'])]

  e) Activate the app (Ref: "Activating the application")
     Add 'catalog.apps.CatalogConfig' to INSTALLED_APPS in
     bookshelf/settings.py

  f) Status field (Ref: "Adding a status field")
     class Status(models.TextChoices):
         DRAFT = 'DF', 'Draft'
         PUBLISHED = 'PB', 'Published'
     status -> CharField(max_length=2, choices=Status, default=Status.DRAFT)

  g) Many-to-one relationship (Ref: "Adding a many-to-one relationship")
     added_by -> ForeignKey(settings.AUTH_USER_MODEL, on_delete=CASCADE,
                             related_name='books_added')
     [represents the cataloger/librarian account responsible for this
     record — intentionally kept many-to-one to mirror Post.author.
     A real-world "book has many authors" relationship is many-to-many
     and is out of scope for Chapter 1]

Deliverable: catalog/models.py with the fully assembled Book model
Definition of done: model imports cleanly, `python manage.py check` passes.

------------------------------------------------------------
PHASE 2b — OPTIONAL / STRETCH: Composite primary key
------------------------------------------------------------
Book ref: "Using the CompositePrimaryKey field"

Task:
[ ] Add a ReaderFavorite model joining User and Book on a composite key:
    pk      -> models.CompositePrimaryKey("reader", "book")
    reader  -> ForeignKey(AUTH_USER_MODEL, on_delete=CASCADE)
    book    -> ForeignKey('catalog.Book', on_delete=CASCADE)
    created -> DateTimeField(auto_now_add=True)

Deliverable: additional model in catalog/models.py, deliberately NOT
registered in admin.py (the book notes CompositePrimaryKey models can't
currently be admin-registered — ticket #35953).

============================================================
PHASE 3 — MIGRATIONS
============================================================
Book ref: "Creating and applying migrations"

Tasks:
[ ] `python manage.py makemigrations catalog`
[ ] `python manage.py sqlmigrate catalog 0001` — inspect generated SQL,
    confirm: CREATE TABLE catalog_book, the descending index on publish,
    the implicit indexes on slug and added_by_id
[ ] `python manage.py migrate`

Deliverable: catalog/migrations/0001_initial.py + a saved copy of the
sqlmigrate output for reference
Definition of done: `migrate` reports "Applying catalog.0001_initial... OK"

============================================================
PHASE 4 — ADMINISTRATION SITE
============================================================
Book ref: "Creating an administration site for models" and all subsections

Tasks:
[ ] `python manage.py createsuperuser`
[ ] Log in at /admin/
[ ] Register the model — start with the plain form (Ref: "Adding models to
    the administration site"):
      admin.site.register(Book)
[ ] Refactor to a customized ModelAdmin (Ref: "Customizing how models are
    displayed"):
      @admin.register(Book)
      class BookAdmin(admin.ModelAdmin):
          list_display = ['title', 'slug', 'added_by', 'publish', 'status']
          list_filter = ['status', 'created', 'publish', 'added_by']
          search_fields = ['title', 'synopsis']
          prepopulated_fields = {'slug': ('title',)}
          raw_id_fields = ['added_by']
          date_hierarchy = 'publish'
          ordering = ['status', 'publish']
[ ] Add facet counts (Ref: "Adding facet counts to filters"):
      show_facets = admin.ShowFacets.ALWAYS
[ ] Create at least 2 books via the admin UI with different statuses and
    added_by users, to visually confirm: autofilled slug, raw-id widget,
    date hierarchy nav, search bar, and filter facet counts

Deliverable: catalog/admin.py
Definition of done: /admin/catalog/book/ shows list_display columns,
sidebar filters with facet counts, and working search.

============================================================
PHASE 5 — ORM & QUERYSET PRACTICE (shell exercises)
============================================================
Book ref: "Working with QuerySets and managers" and all subsections

Run all of the following in `python manage.py shell` and keep a transcript.

[ ] Creating objects (Ref: "Creating objects")
    - User.objects.get(username=...), build Book(...) in memory, .save()
    - Book.objects.create(...)
    - User.objects.get_or_create(username=...)
[ ] Updating objects (Ref: "Updating objects")
    - mutate book.title, call .save() again
[ ] Retrieving objects (Ref: "Retrieving objects")
    - Book.objects.all()
[ ] Filtering + field lookups (Ref: "Filtering objects", "Using field
    lookups")
    - exact, iexact, contains/icontains, in, gt/gte/lt/lte,
      startswith/istartswith, endswith/iendswith
    - date lookups on `publish`: __date, __year, __month, __day, __date__gt
    - related-field lookup: added_by__username
[ ] Chaining filters (Ref: "Chaining filters")
[ ] Excluding objects (Ref: "Excluding objects")
[ ] Ordering objects (Ref: "Ordering objects")
    - order_by('title'), order_by('-title'), order_by('added_by','title'),
      order_by('?')
[ ] Limiting QuerySets (Ref: "Limiting QuerySets") — slicing, e.g. [:5]
[ ] Counting objects (Ref: "Counting objects") — .count()
[ ] Checking existence (Ref: "Checking if an object exists") — .exists()
[ ] Deleting objects (Ref: "Deleting objects") — .delete()
[ ] Complex lookups with Q objects (Ref: "Complex lookups with Q objects")
    - OR query: titles starting with two different prefixes
[ ] QuerySet laziness (Ref: "When QuerySets are evaluated")
    - demonstrate an unevaluated QuerySet vs. forcing evaluation with
      list(), len(), bool(), or iteration
[ ] Custom manager (Ref: "Creating model managers")
    - class PublishedManager(models.Manager): filter status=PUBLISHED
    - expose as Book.published, keep Book.objects as default

Deliverable: shell_session_notes.txt (commands + real output) and the
PublishedManager added to catalog/models.py
Definition of done: Book.published.all() returns only PUBLISHED books.

============================================================
PHASE 6 — VIEWS, URLS & TEMPLATES
============================================================
Book ref: "Building list and detail views" and all subsections

Tasks:
[ ] catalog/views.py
    - book_list(request): Book.published.all(), render
      'catalog/book/list.html'
    - book_detail(request, id): get_object_or_404(Book, id=id,
      status=Book.Status.PUBLISHED), render 'catalog/book/detail.html'
      (Ref: "Using the get_object_or_404 shortcut")
[ ] catalog/urls.py (Ref: "Adding URL patterns for your views")
    - app_name = 'catalog'
    - path('', views.book_list, name='book_list')
    - path('<int:id>/', views.book_detail, name='book_detail')
[ ] bookshelf/urls.py — include catalog.urls under 'catalog/' with
    namespace='catalog'
[ ] templates/catalog/base.html (Ref: "Creating a base template")
    - {% load static %}, {% block title %}, {% block content %}, sidebar
[ ] templates/catalog/book/list.html (Ref: "Creating the post list
    template")
    - extends base, loop over books, link via
      {% url 'catalog:book_detail' book.id %}, apply
      {{ book.synopsis|truncatewords:30|linebreaks }}
[ ] templates/catalog/book/detail.html (Ref: "Creating the post detail
    template")
[ ] Verify (Ref: "Accessing our application")
    - http://127.0.0.1:8000/catalog/ shows published books
    - http://127.0.0.1:8000/catalog/<id>/ shows a single book

Deliverable: views.py, urls.py (app + project), 3 templates, working routes
Definition of done: both list and detail pages render live data with
working links.

============================================================
PHASE 7 — REQUEST/RESPONSE REVIEW & WRAP-UP
============================================================
Book ref: "The request/response cycle", "Management commands used in this
          chapter"

Tasks:
[ ] Trace one full request to /catalog/3/ in your own words for the
    BookShelf domain: URL dispatch -> book_detail view -> Book.published
    ORM query -> detail.html render -> HttpResponse (mirrors Figure 1.22)
[ ] Compile the management-commands cheat sheet below into your own notes

Deliverable: a one-page written walkthrough + the cheat sheet
Definition of done: you can explain the cycle without looking at the book.

============================================================
MANAGEMENT COMMAND CHEAT SHEET (bookshelf/catalog equivalents)
============================================================
django-admin startproject bookshelf
python manage.py startapp catalog
python manage.py makemigrations catalog
python manage.py sqlmigrate catalog 0001
python manage.py migrate
python manage.py runserver
python manage.py shell
python manage.py createsuperuser

============================================================
DOMAIN MAPPING TABLE (Blog -> BookShelf)
============================================================
mysite project        -> bookshelf project
blog app               -> catalog app
Post model             -> Book model
Post.title             -> Book.title
Post.slug              -> Book.slug
Post.author (FK->User) -> Book.added_by (FK->User)
Post.body               -> Book.synopsis
Post.publish            -> Book.publish
Post.created            -> Book.created
Post.updated            -> Book.updated
Post.status              -> Book.status
related_name 'blog_posts' -> related_name 'books_added'
PublishedManager          -> PublishedManager
post_list / post_detail   -> book_list / book_detail
blog:post_list             -> catalog:book_list
blog:post_detail            -> catalog:book_detail
FavouritePost (composite PK example) -> ReaderFavorite (composite PK example)

============================================================
OUT OF SCOPE (deliberately excluded — covered in later chapters)
============================================================
- Pagination
- Class-based views
- Canonical / SEO-friendly URLs
- Forms (share-by-email, comments)
- Tagging, related-post recommendations, search
- Any many-to-many "real" book-authors relationship

END OF ROADMAP

BOOKSHELF CMS — CHAPTER 2 IMPLEMENTATION ROADMAP
Mirrors: "Django 5 by Example", Chapter 2 — Enhancing Your Blog and Adding
         Social Features
Prerequisite: bookshelf_cms_chapter1_roadmap.txt, Phases 0-7 complete
(bookshelf project, catalog app, Book model, admin, book_list/book_detail
views+urls+templates all working).

Scope rule: every task below maps 1:1 to a section in Chapter 2. Nothing
from Chapter 3+ (tagging, similarity recommendations, custom template
tags/filters, sitemaps, feeds, full-text search) is included on purpose —
see OUT OF SCOPE at the end.

============================================================
0. NEW NAMING FOR THIS CHAPTER
============================================================
EmailPostForm  -> EmailBookForm     (share a book via email)
post_share      -> book_share
Comment model   -> Review model     (readers review books, not "comment"
                                      on a catalog entry — see the note in
                                      Phase 13 for why this is the one
                                      deliberate rename this chapter)
CommentForm     -> ReviewForm
post_comment    -> book_review
comment.html    -> review.html
comment_form.html -> review_form.html
post.comments   -> book.reviews

Target end-state directory tree additions (on top of Chapter 1's tree):

bookshelf/
    .env
    requirements.txt        (now: Django~=5.2, python-decouple==3.8)
    .gitignore                (now includes .env)
    catalog/
        admin.py              (+ ReviewAdmin)
        forms.py               (new: EmailBookForm, ReviewForm)
        models.py               (+ get_absolute_url, unique_for_date, Review)
        views.py                (+ book_share, book_review, BookListView)
        urls.py                  (+ share/review paths, date+slug detail path)
        migrations/
            0002_alter_book_slug.py
            0003_review.py
        templates/
            pagination.html      (generic — lives directly under
                                   catalog/templates/, NOT namespaced under
                                   catalog/templates/catalog/, same as the
                                   book's blog/templates/pagination.html)
            catalog/
                book/
                    share.html
                    review.html
                    includes/
                        review_form.html

============================================================
PHASE 8 — CANONICAL URLS
============================================================
Book ref: "Using canonical URLs for models"

Tasks:
[ ] catalog/models.py — import reverse from django.urls
[ ] Add to Book:
      def get_absolute_url(self):
          return reverse('catalog:book_detail', args=[self.id])
[ ] catalog/book/list.html — replace
      <a href="{% url 'catalog:book_detail' book.id %}">
    with
      <a href="{{ book.get_absolute_url }}">

Deliverable: updated models.py + list.html
Definition of done: links from the list page to the detail page still
resolve correctly, now built through get_absolute_url() instead of a
hardcoded {% url %} call.

============================================================
PHASE 9 — SEO-FRIENDLY URLS
============================================================
Book ref: "Creating SEO-friendly URLs for posts", "Modifying the URL
          patterns", "Modifying the views", "Modifying the canonical URL
          for posts"

Tasks:
[ ] catalog/models.py — add unique_for_date to Book.slug:
      slug = models.SlugField(max_length=250, unique_for_date='publish')
    (not enforced at the DB level, but Django still needs a migration to
    track the model-state change)
[ ] `python manage.py makemigrations catalog` -> 0002_alter_book_slug.py
[ ] `python manage.py migrate`
[ ] catalog/urls.py — replace
      path('<int:id>/', views.book_detail, name='book_detail')
    with
      path(
          '<int:year>/<int:month>/<int:day>/<slug:book>/',
          views.book_detail,
          name='book_detail'
      )
[ ] catalog/views.py — update book_detail signature and lookup:
      def book_detail(request, year, month, day, book):
          book = get_object_or_404(
              Book,
              status=Book.Status.PUBLISHED,
              slug=book,
              publish__year=year,
              publish__month=month,
              publish__day=day
          )
          return render(request, 'catalog/book/detail.html', {'book': book})
    (note the parameter/variable name `book` is intentionally reused for
    both the incoming slug string and the resolved object — this mirrors
    the book's own `post` reuse in post_detail exactly)
[ ] catalog/models.py — update get_absolute_url():
      def get_absolute_url(self):
          return reverse(
              'catalog:book_detail',
              args=[
                  self.publish.year,
                  self.publish.month,
                  self.publish.day,
                  self.slug,
              ]
          )

Deliverable: updated models.py, urls.py, views.py; new migration file
Definition of done: a book detail URL looks like
/catalog/2026/7/6/dune/ and resolves to the right book.

============================================================
PHASE 10 — PAGINATION
============================================================
Book ref: "Adding pagination", "Adding pagination to the post list view",
          "Creating a pagination template", "Handling pagination errors"

Tasks:
[ ] catalog/views.py — import Paginator, EmptyPage, PageNotAnInteger;
    rewrite book_list:
      def book_list(request):
          book_list = Book.published.all()
          paginator = Paginator(book_list, 3)   # 3 books per page
          page_number = request.GET.get('page', 1)
          try:
              books = paginator.page(page_number)
          except PageNotAnInteger:
              books = paginator.page(1)
          except EmptyPage:
              books = paginator.page(paginator.num_pages)
          return render(request, 'catalog/book/list.html', {'books': books})
[ ] Create catalog/templates/pagination.html (generic — same file the book
    uses, no BookShelf-specific naming needed):
      <div class="pagination">
        <span class="step-links">
          {% if page.has_previous %}
            <a href="?page={{ page.previous_page_number }}">Previous</a>
          {% endif %}
          <span class="current">
            Page {{ page.number }} of {{ page.paginator.num_pages }}.
          </span>
          {% if page.has_next %}
            <a href="?page={{ page.next_page_number }}">Next</a>
          {% endif %}
        </span>
      </div>
[ ] catalog/book/list.html — include it:
      {% include "pagination.html" with page=books %}
[ ] Via /admin/, create at least 4 Published books to force a second page
[ ] Manually verify:
      ?page=2      -> second page of results
      ?page=99     -> EmptyPage handled, falls back to last page
      ?page=abc    -> PageNotAnInteger handled, falls back to page 1

Deliverable: updated views.py, new pagination.html, updated list.html
Definition of done: paging works, and both error cases degrade gracefully
instead of throwing an unhandled exception page.

============================================================
PHASE 11 — CLASS-BASED LIST VIEW
============================================================
Book ref: "Building class-based views", "Why use class-based views", "Using
          a class-based view to list posts"

Tasks:
[ ] catalog/views.py:
      from django.views.generic import ListView
      class BookListView(ListView):
          """Alternative book list view"""
          queryset = Book.published.all()
          context_object_name = 'books'
          paginate_by = 3
          template_name = 'catalog/book/list.html'
[ ] catalog/urls.py — comment out (don't delete — keep it for comparison,
    same as the book does) the function-based route and add:
      # path('', views.book_list, name='book_list'),
      path('', views.BookListView.as_view(), name='book_list'),
[ ] catalog/book/list.html — ListView passes the page as `page_obj`, not
    the `books` variable used in Phase 10, so update the include:
      {% include "pagination.html" with page=page_obj %}

Deliverable: BookListView in views.py, updated urls.py and list.html
Definition of done: pagination still works end-to-end. Confirm you can
explain why ?page=99 or ?page=abc now returns an HTTP 404 instead of the
custom fallback behavior from Phase 10 — that's ListView's built-in error
handling replacing your manual try/except.

============================================================
PHASE 12 — EMAIL RECOMMENDATIONS ("Share this book")
============================================================
Book ref: "Recommending posts by email", "Creating forms with Django",
          "Handling forms in views", "Sending emails with Django", "Working
          with environment variables", "Sending emails in views",
          "Rendering forms in templates"

Tasks:
[ ] catalog/forms.py (new file):
      from django import forms
      class EmailBookForm(forms.Form):
          name = forms.CharField(max_length=25)
          email = forms.EmailField()
          to = forms.EmailField()
          comments = forms.CharField(required=False, widget=forms.Textarea)
    Note: the book also calls this field `comments` — it's just the
    optional personal note attached to a shared-book email, unrelated to
    the Review model built in Phase 13. Kept as-is here for a strict
    mirror; rename to `note` in your own code if the overlap with
    "Review" bothers you.

[ ] catalog/views.py — book_share view (Ref: "Handling forms in views"):
      def book_share(request, book_id):
          book = get_object_or_404(Book, id=book_id, status=Book.Status.PUBLISHED)
          sent = False
          if request.method == 'POST':
              form = EmailBookForm(request.POST)
              if form.is_valid():
                  cd = form.cleaned_data
                  # ... send email (added below)
                  sent = True
          else:
              form = EmailBookForm()
          return render(request, 'catalog/book/share.html',
                        {'book': book, 'form': form, 'sent': sent})

[ ] `python -m pip install python-decouple==3.8`
[ ] Add python-decouple==3.8 and Django~=5.2 to requirements.txt
[ ] Create .env in the project root (Ref: "Working with environment
    variables"):
      EMAIL_HOST_USER=you@gmail.com
      EMAIL_HOST_PASSWORD=
      DEFAULT_FROM_EMAIL=BookShelf CMS <you@gmail.com>
[ ] Add .env to .gitignore
[ ] bookshelf/settings.py:
      from decouple import config
      EMAIL_HOST = 'smtp.gmail.com'
      EMAIL_HOST_USER = config('EMAIL_HOST_USER')
      EMAIL_HOST_PASSWORD = config('EMAIL_HOST_PASSWORD')
      EMAIL_PORT = 587
      EMAIL_USE_TLS = True
      DEFAULT_FROM_EMAIL = config('DEFAULT_FROM_EMAIL')
    (or, without a real SMTP account:
      EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
    to print emails to the console instead)
[ ] If using Gmail: enable 2-Step Verification, generate an app password at
    https://myaccount.google.com/apppasswords, put it in EMAIL_HOST_PASSWORD
[ ] Shell-test (Ref: "Sending emails with Django"):
      python manage.py shell
      >>> from django.core.mail import send_mail
      >>> send_mail('Test', 'Hello from BookShelf', None, ['you@gmail.com'],
      ...           fail_silently=False)

[ ] Wire the real send in book_share (Ref: "Sending emails in views"):
      from django.core.mail import send_mail
      post_url = request.build_absolute_uri(book.get_absolute_url())
      subject = f"{cd['name']} ({cd['email']}) recommends you read {book.title}"
      message = (
          f"Read {book.title} at {post_url}\n\n"
          f"{cd['name']}'s comments: {cd['comments']}"
      )
      send_mail(subject=subject, message=message, from_email=None,
                recipient_list=[cd['to']])
[ ] catalog/urls.py:
      path('<int:book_id>/share/', views.book_share, name='book_share')
[ ] templates/catalog/book/share.html (Ref: "Rendering forms in templates"):
      {% extends "catalog/base.html" %}
      {% block title %}Share a book{% endblock %}
      {% block content %}
        {% if sent %}
          <h1>E-mail successfully sent</h1>
          <p>"{{ book.title }}" was successfully sent to
             {{ form.cleaned_data.to }}.</p>
        {% else %}
          <h1>Share "{{ book.title }}" by e-mail</h1>
          <form method="post">
            {{ form.as_p }}
            {% csrf_token %}
            <input type="submit" value="Send e-mail">
          </form>
        {% endif %}
      {% endblock %}
[ ] catalog/book/detail.html — add a share link:
      <a href="{% url 'catalog:book_share' book.id %}">Share this book</a>

Deliverable: forms.py (EmailBookForm), book_share view, urls.py entry,
share.html, .env + settings.py email config
Definition of done: sharing a book by email either lands in your inbox
(real SMTP) or prints to the console (EmailBackend), the success message
renders on submit, and invalid input redisplays the form with errors.

============================================================
PHASE 13 — REVIEW SYSTEM (mirrors the book's Comment system)
============================================================
Book ref: "Creating a comment system", "Creating a model for comments",
          "Adding comments to the administration site", "Creating forms
          from models", "Handling ModelForms in views", "Creating templates
          for the comment form", "Adding comments to the post detail view",
          "Adding comments to the post detail template", "Using simplified
          templates for form rendering"

Note on naming: this is the one deliberate rename in Chapter 2. The book's
generic "Comment" concept maps naturally onto "Review" for a book catalog
— readers review books, they don't leave freeform comments on a catalog
entry. Everything about the model/form/view/template structure below is
otherwise identical to the book's Comment system, field for field.

Tasks:
[ ] catalog/models.py — Review model (Ref: "Creating a model for
    comments"):
      class Review(models.Model):
          book = models.ForeignKey(
              Book, on_delete=models.CASCADE, related_name='reviews'
          )
          name = models.CharField(max_length=80)
          email = models.EmailField()
          body = models.TextField()
          created = models.DateTimeField(auto_now_add=True)
          updated = models.DateTimeField(auto_now=True)
          active = models.BooleanField(default=True)
          class Meta:
              ordering = ['created']
              indexes = [models.Index(fields=['created'])]
          def __str__(self):
              return f'Review by {self.name} on {self.book}'
[ ] `python manage.py makemigrations catalog` -> 0003_review.py
[ ] `python manage.py migrate`
[ ] catalog/admin.py (Ref: "Adding comments to the administration site"):
      from .models import Book, Review
      @admin.register(Review)
      class ReviewAdmin(admin.ModelAdmin):
          list_display = ['name', 'email', 'book', 'created', 'active']
          list_filter = ['active', 'created', 'updated']
          search_fields = ['name', 'email', 'body']
[ ] catalog/forms.py — ReviewForm (Ref: "Creating forms from models"):
      from .models import Review
      class ReviewForm(forms.ModelForm):
          class Meta:
              model = Review
              fields = ['name', 'email', 'body']
    (optional stretch, Ref: "Using simplified templates for form
    rendering": mirror the book's custom BoundField / as_field_group
    styling hook to wrap review fields in a styled div)
[ ] catalog/views.py — book_review view (Ref: "Handling ModelForms in
    views"):
      from django.views.decorators.http import require_POST
      from .forms import ReviewForm
      @require_POST
      def book_review(request, book_id):
          book = get_object_or_404(
              Book, id=book_id, status=Book.Status.PUBLISHED
          )
          review = None
          form = ReviewForm(data=request.POST)
          if form.is_valid():
              review = form.save(commit=False)
              review.book = book
              review.save()
          return render(
              request,
              'catalog/book/review.html',
              {'book': book, 'form': form, 'review': review}
          )
[ ] catalog/urls.py:
      path('<int:book_id>/review/', views.book_review, name='book_review')
[ ] templates/catalog/book/includes/review_form.html (Ref: "Creating
    templates for the comment form"):
      <h2>Add a new review</h2>
      <form action="{% url 'catalog:book_review' book.id %}" method="post">
        {{ form.as_p }}
        {% csrf_token %}
        <p><input type="submit" value="Add review"></p>
      </form>
[ ] templates/catalog/book/review.html:
      {% extends "catalog/base.html" %}
      {% block title %}Add a review{% endblock %}
      {% block content %}
        {% if review %}
          <h2>Your review has been added.</h2>
          <p><a href="{{ book.get_absolute_url }}">Back to the book</a></p>
        {% else %}
          {% include "catalog/book/includes/review_form.html" %}
        {% endif %}
      {% endblock %}
[ ] catalog/views.py — update book_detail (Ref: "Adding comments to the
    post detail view") to also pass reviews + a blank form:
      reviews = book.reviews.filter(active=True)
      form = ReviewForm()
      # add 'reviews': reviews, 'form': form to the render() context
[ ] catalog/book/detail.html (Ref: "Adding comments to the post detail
    template") — add:
      {% with reviews.count as total_reviews %}
        <h2>{{ total_reviews }} review{{ total_reviews|pluralize }}</h2>
      {% endwith %}
      {% for review in reviews %}
        <div class="review">
          <p class="info">
            Review {{ forloop.counter }} by {{ review.name }}
            {{ review.created }}
          </p>
          {{ review.body|linebreaks }}
        </div>
      {% empty %}
        <p>There are no reviews.</p>
      {% endfor %}
      {% include "catalog/book/includes/review_form.html" %}

Deliverable: Review model + migration, ReviewAdmin, ReviewForm, book_review
view, review.html + includes/review_form.html, updated book_detail view and
template
Definition of done: submitting a review redirects to a success message;
the book detail page lists active reviews with a live, correctly
pluralized count; deactivating a review in /admin/ removes it from the
public page (and from the count) without deleting the row.

============================================================
MANAGEMENT COMMAND CHEAT SHEET — CHAPTER 2 ADDITIONS
============================================================
python -m pip install python-decouple==3.8
python manage.py makemigrations catalog     # 0002_alter_book_slug, 0003_review
python manage.py migrate
python manage.py shell                       # for the send_mail() smoke test

============================================================
DOMAIN MAPPING TABLE — CHAPTER 2 ADDITIONS
============================================================
Post.get_absolute_url()        -> Book.get_absolute_url()
unique_for_date='publish'       -> unique_for_date='publish' (unchanged concept)
post_list (paginated)            -> book_list (paginated)
PostListView                      -> BookListView
EmailPostForm                      -> EmailBookForm
post_share                          -> book_share
Comment model                        -> Review model
CommentForm                           -> ReviewForm
post_comment                           -> book_review
post.comments (related_name)            -> book.reviews (related_name)
blog/post/share.html                     -> catalog/book/share.html
blog/post/comment.html                    -> catalog/book/review.html
blog/post/includes/comment_form.html       -> catalog/book/includes/review_form.html
templates/pagination.html (generic)         -> templates/pagination.html (unchanged)

============================================================
OUT OF SCOPE (deliberately excluded — covered in Chapter 3)
============================================================
- Tagging system for books (django-taggit equivalent)
- "Readers who liked this also liked..." QuerySet-similarity recommendations
- Custom template tags and filters
- Sitemap generation
- RSS/Atom feed for new books
- Full-text search across titles/synopses

END OF ROADMAP

BOOKSHELF CMS — CHAPTER 3 IMPLEMENTATION ROADMAP
Mirrors: "Django 5 by Example", Chapter 3 — Extending Your Blog Application
Prerequisite: bookshelf_cms_chapter1_roadmap.txt (Phases 0-7) and
bookshelf_cms_chapter2_roadmap.txt (Phases 8-13) complete — Book model,
Review model, admin, pagination, BookListView, email sharing all working.

Scope rule: every task below maps 1:1 to a section in Chapter 3. Nothing
from Chapter 4+ (authentication, registration, custom user profiles,
password reset, social auth) is included on purpose — see OUT OF SCOPE at
the end.

============================================================
0. NEW NAMING FOR THIS CHAPTER
============================================================
django-taggit `tags` manager  -> unchanged name (`tags`) on Book — see the
                                  note in Phase 14 for why this one is NOT
                                  renamed to "genres"
post_tags_ids / similar_posts   -> book_tags_ids / similar_books
templatetags/blog_tags.py        -> templatetags/catalog_tags.py
total_posts                       -> total_books
show_latest_posts                  -> show_latest_books
get_most_commented_posts             -> get_most_reviewed_books
                                        (Count('comments') -> Count('reviews'))
blog/post/latest_posts.html            -> catalog/book/latest_books.html
PostSitemap                              -> BookSitemap
LatestPostsFeed                           -> LatestBooksFeed
post_feed                                  -> book_feed
post_search                                 -> book_search
blog/post/search.html                        -> catalog/book/search.html
blog_db (Docker container / DB name/user)      -> bookshelf_db / bookshelf
mysite_data.json (fixture dump)                  -> bookshelf_data.json

Target end-state directory tree additions (on top of Chapters 1-2's tree):

bookshelf/
    .env                        (+ DB_NAME, DB_USER, DB_PASSWORD, DB_HOST)
    bookshelf_data.json         (fixture dump from the SQLite -> Postgres move)
    requirements.txt            (+ django-taggit==5.0.1, markdown==3.6,
                                   psycopg==3.1.18)
    bookshelf/
        settings.py             (+ SITE_ID, taggit / sites / sitemaps /
                                   postgres in INSTALLED_APPS, DATABASES
                                   switched to postgresql)
        urls.py                  (+ sitemap.xml route)
    catalog/
        feeds.py                 (new: LatestBooksFeed)
        sitemaps.py                (new: BookSitemap)
        forms.py                    (+ SearchForm)
        models.py                    (+ tags = TaggableManager())
        views.py                      (+ tag filtering, similar_books,
                                          book_search)
        urls.py                        (+ tag/<slug:tag_slug>/, feed/,
                                          search/ paths; book_list reverted
                                          to function-based, see Phase 14)
        migrations/
            0004_book_tags.py
            0005_trigram_ext.py
        templatetags/
            __init__.py
            catalog_tags.py
        templates/
            catalog/
                base.html            (+ {% load catalog_tags %}, sidebar
                                        widgets, RSS subscribe link)
                book/
                    latest_books.html  (new — inclusion-tag template)
                    search.html         (new)
                    list.html            (+ tag display/filter, markdown
                                            filter on synopsis)
                    detail.html           (+ similar books, markdown filter)

============================================================
PHASE 14 — TAGGING WITH DJANGO-TAGGIT
============================================================
Book ref: "Implementing tagging with django-taggit"

Tasks:
[ ] `python -m pip install django-taggit==5.0.1`
[ ] bookshelf/settings.py — add 'taggit' to INSTALLED_APPS, above
    'catalog.apps.CatalogConfig' (Django apps first, third-party in the
    middle, local apps last)
[ ] catalog/models.py:
      from taggit.managers import TaggableManager
      class Book(models.Model):
          # ...
          tags = TaggableManager()
    Note: kept as `tags`, not renamed to `genres` — a book catalog
    legitimately wants free-form tags beyond genre (e.g. "award-winner",
    "kenyan-author", "book-club-pick"), so the generic name still fits.
    This mirrors Phase 13's rule: rename only when the concept itself
    changes, not just the label.
[ ] `python manage.py makemigrations catalog` -> 0004_book_tags.py
[ ] `python manage.py migrate` (creates taggit's own tables too)
[ ] Shell-test (Ref: same section):
      python manage.py shell
      >>> from catalog.models import Book
      >>> book = Book.objects.get(id=1)
      >>> book.tags.add('sci-fi', 'space-opera', 'classic')
      >>> book.tags.all()
      >>> book.tags.remove('classic')
    (try genre-flavored tags relevant to your own catalog — e.g.
    'kenyan-fiction', 'east-african-literature' work just as well as the
    book's music/jazz examples)
[ ] Confirm /admin/taggit/tag/ lists your tags and /admin/catalog/book/<id>/
    change/ shows the new Tags field
[ ] catalog/book/list.html — display tags per book:
      <p class="tags">Tags: {{ book.tags.all|join:", " }}</p>

[ ] catalog/views.py — filter book_list by an optional tag (Ref: same
    section):
      from taggit.models import Tag
      def book_list(request, tag_slug=None):
          book_list = Book.published.all()
          tag = None
          if tag_slug:
              tag = get_object_or_404(Tag, slug=tag_slug)
              book_list = book_list.filter(tags__in=[tag])
          paginator = Paginator(book_list, 3)
          page_number = request.GET.get('page', 1)
          try:
              books = paginator.page(page_number)
          except PageNotAnInteger:
              books = paginator.page(1)
          except EmptyPage:
              books = paginator.page(paginator.num_pages)
          return render(request, 'catalog/book/list.html',
                        {'books': books, 'tag': tag})
[ ] catalog/urls.py — this reverts the class-based swap from Phase 11
    (ListView can't easily take the extra tag_slug argument without more
    plumbing, so the book itself switches back to the function view here):
      path('', views.book_list, name='book_list'),
      # path('', views.BookListView.as_view(), name='book_list'),
      path(
          'tag/<slug:tag_slug>/', views.book_list, name='book_list_by_tag'
      ),
[ ] catalog/book/list.html — revert the pagination include back to the
    `books` context var (function-based view, not `page_obj`):
      {% include "pagination.html" with page=books %}
    and show the active tag + per-tag links:
      {% if tag %}
        <h2>Books tagged with "{{ tag.name }}"</h2>
      {% endif %}
      ...
      <p class="tags">
        Tags:
        {% for tag in book.tags.all %}
          <a href="{% url 'catalog:book_list_by_tag' tag.slug %}">
            {{ tag.name }}
          </a>{% if not forloop.last %}, {% endif %}
        {% endfor %}
      </p>

Deliverable: taggit installed and migrated, Book.tags manager, tag display
and tag-filtered list page, book_list reverted to function-based
Definition of done: /catalog/tag/sci-fi/ shows only books tagged sci-fi;
clicking a tag on any book takes you there.

============================================================
PHASE 15 — RETRIEVING BOOKS BY SIMILARITY
============================================================
Book ref: "Retrieving posts by similarity"

Tasks:
[ ] catalog/views.py:
      from django.db.models import Count
[ ] Update book_detail (the date+slug version from Chapter 2 Phase 9) to
    add a similar-books lookup:
      book_tags_ids = book.tags.values_list('id', flat=True)
      similar_books = Book.published.filter(
          tags__in=book_tags_ids
      ).exclude(id=book.id)
      similar_books = similar_books.annotate(
          same_tags=Count('tags')
      ).order_by('-same_tags', '-publish')[:4]
    Pass similar_books into the render() context alongside reviews/form.
[ ] catalog/book/detail.html — add a "Similar books" section:
      <h2>Similar books</h2>
      {% for book in similar_books %}
        <p><a href="{{ book.get_absolute_url }}">{{ book.title }}</a></p>
      {% empty %}
        There are no similar books yet.
      {% endfor %}
[ ] Via /admin/, tag two or more books with an overlapping genre tag (e.g.
    both tagged 'sci-fi' and 'dystopian') and confirm they recommend each
    other

Deliverable: updated book_detail view + detail.html
Definition of done: opening a tagged book's detail page lists other
published books ordered by number of shared tags, then by most recent.

============================================================
PHASE 16 — CUSTOM TEMPLATE TAGS
============================================================
Book ref: "Creating custom template tags and filters", "Implementing
          custom template tags", "Creating a simple template tag",
          "Creating an inclusion template tag", "Creating a template tag
          that returns a QuerySet"

Tasks:
[ ] Create catalog/templatetags/ with an empty __init__.py and a
    catalog_tags.py file
[ ] Simple tag (Ref: "Creating a simple template tag"):
      from django import template
      from ..models import Book
      register = template.Library()
      @register.simple_tag
      def total_books():
          return Book.published.count()
[ ] catalog/base.html — load and use it:
      {% load catalog_tags %}
      ...
      I've catalogued {% total_books %} books so far.
    (restart the dev server after adding a new templatetags module)
[ ] Inclusion tag (Ref: "Creating an inclusion template tag"):
      @register.inclusion_tag('catalog/book/latest_books.html')
      def show_latest_books(count=5):
          latest_books = Book.published.order_by('-publish')[:count]
          return {'latest_books': latest_books}
[ ] templates/catalog/book/latest_books.html:
      <ul>
        {% for book in latest_books %}
          <li><a href="{{ book.get_absolute_url }}">{{ book.title }}</a></li>
        {% endfor %}
      </ul>
[ ] catalog/base.html:
      <h3>Latest books</h3>
      {% show_latest_books 3 %}
[ ] QuerySet-returning tag (Ref: "Creating a template tag that returns a
    QuerySet"):
      from django.db.models import Count
      @register.simple_tag
      def get_most_reviewed_books(count=5):
          return Book.published.annotate(
              total_reviews=Count('reviews')
          ).order_by('-total_reviews')[:count]
[ ] catalog/base.html:
      <h3>Most reviewed books</h3>
      {% get_most_reviewed_books as most_reviewed_books %}
      <ul>
        {% for book in most_reviewed_books %}
          <li><a href="{{ book.get_absolute_url }}">{{ book.title }}</a></li>
        {% endfor %}
      </ul>

Deliverable: catalog/templatetags/catalog_tags.py with total_books,
show_latest_books, get_most_reviewed_books; latest_books.html; updated
base.html sidebar
Definition of done: every page's sidebar shows a live book count, the
latest 3 books, and the 3 most-reviewed books, all without touching the
view functions that render those pages.

============================================================
PHASE 17 — CUSTOM TEMPLATE FILTER (MARKDOWN)
============================================================
Book ref: "Implementing custom template filters", "Creating a template
          filter to support Markdown syntax"

Tasks:
[ ] `python -m pip install markdown==3.6`
[ ] catalog/templatetags/catalog_tags.py:
      import markdown
      from django.utils.safestring import mark_safe
      @register.filter(name='markdown')
      def markdown_format(text):
          return mark_safe(markdown.markdown(text))
[ ] catalog/book/detail.html — replace the synopsis's `linebreaks` filter
    with `markdown`:
      {{ book.synopsis|markdown }}
    (add {% load catalog_tags %} to detail.html if not already loaded)
[ ] catalog/book/list.html — same swap, keeping the truncation HTML-safe:
      {{ book.synopsis|markdown|truncatewords_html:30 }}
[ ] Via /admin/, create or edit a book with a Markdown-formatted synopsis
    (headings, *emphasis*, a bullet list, a link) and confirm it renders
    as HTML on both the list and detail pages

Deliverable: markdown filter in catalog_tags.py, updated list.html and
detail.html
Definition of done: Markdown in a book's synopsis renders as formatted
HTML; plain text still displays correctly. Remember: storing Markdown
(not HTML) in the synopsis field is the safer choice — it limits what can
be injected, since formatting is only converted to HTML at render time.

============================================================
PHASE 18 — SITEMAP
============================================================
Book ref: "Adding a sitemap to the site"

Tasks:
[ ] bookshelf/settings.py:
      SITE_ID = 1
      INSTALLED_APPS = [
          'django.contrib.admin',
          'django.contrib.auth',
          'django.contrib.contenttypes',
          'django.contrib.sessions',
          'django.contrib.messages',
          'django.contrib.sites',
          'django.contrib.sitemaps',
          'django.contrib.staticfiles',
          'taggit',
          'catalog.apps.CatalogConfig',
      ]
[ ] `python manage.py migrate` (applies sites.0001_initial,
    sites.0002_alter_domain_unique)
[ ] catalog/sitemaps.py (new file):
      from django.contrib.sitemaps import Sitemap
      from .models import Book
      class BookSitemap(Sitemap):
          changefreq = 'weekly'
          priority = 0.9
          def items(self):
              return Book.published.all()
          def lastmod(self, obj):
              return obj.updated
[ ] bookshelf/urls.py:
      from django.contrib.sitemaps.views import sitemap
      from catalog.sitemaps import BookSitemap
      sitemaps = {'books': BookSitemap}
      urlpatterns = [
          ...
          path(
              'sitemap.xml',
              sitemap,
              {'sitemaps': sitemaps},
              name='django.contrib.sitemaps.views.sitemap'
          ),
      ]
[ ] Open /admin/sites/site/, edit the default Site object, set the domain
    to localhost:8000 so sitemap URLs resolve locally

Deliverable: catalog/sitemaps.py, updated bookshelf/urls.py, Site domain
set to localhost:8000
Definition of done: /sitemap.xml lists every published book's canonical
URL with a <lastmod> matching its `updated` timestamp.

============================================================
PHASE 19 — RSS FEED
============================================================
Book ref: "Creating feeds for blog posts"

Tasks:
[ ] catalog/feeds.py (new file):
      import markdown
      from django.contrib.syndication.views import Feed
      from django.template.defaultfilters import truncatewords_html
      from django.urls import reverse_lazy
      from .models import Book
      class LatestBooksFeed(Feed):
          title = 'BookShelf CMS'
          link = reverse_lazy('catalog:book_list')
          description = 'Newly added books.'
          def items(self):
              return Book.published.all()[:5]
          def item_title(self, item):
              return item.title
          def item_description(self, item):
              return truncatewords_html(
                  markdown.markdown(item.synopsis), 30
              )
          def item_pubdate(self, item):
              return item.publish
[ ] catalog/urls.py:
      from .feeds import LatestBooksFeed
      path('feed/', LatestBooksFeed(), name='book_feed'),
[ ] catalog/base.html — add a subscribe link:
      <a href="{% url 'catalog:book_feed' %}">Subscribe to new books</a>
[ ] Verify /catalog/feed/ renders valid RSS XML with your 5 most recent
    published books
[ ] Optional: point an RSS reader (e.g. Fluent Reader) at
    http://127.0.0.1:8000/catalog/feed/ to confirm it's readable outside
    the browser

Deliverable: catalog/feeds.py, updated urls.py and base.html
Definition of done: /catalog/feed/ is valid RSS with correct titles, HTML
-stripped-and-truncated descriptions, and publish dates.

============================================================
PHASE 20 — POSTGRESQL SETUP & DATA MIGRATION
============================================================
Book ref: "Installing Docker", "Installing PostgreSQL", "Dumping the
          existing data", "Switching the database in the project",
          "Loading the data into the new database"

Tasks:
[ ] Install Docker Desktop / CLI for your OS
[ ] `docker pull postgres:16.2`
[ ] Start the container:
      docker run --name=bookshelf_db -e POSTGRES_DB=bookshelf \
        -e POSTGRES_USER=bookshelf -e POSTGRES_PASSWORD=xxxxx \
        -p 5432:5432 -d postgres:16.2
    (replace xxxxx with your own password)
[ ] `python -m pip install psycopg==3.1.18`
[ ] Dump the current SQLite data (Ref: "Dumping the existing data"):
      python manage.py dumpdata --indent=2 --output=bookshelf_data.json
    (use `python -Xutf8 manage.py dumpdata ...` if you hit an encoding
    error)
[ ] bookshelf/settings.py — switch DATABASES (Ref: "Switching the
    database in the project"):
      DATABASES = {
          'default': {
              'ENGINE': 'django.db.backends.postgresql',
              'NAME': config('DB_NAME'),
              'USER': config('DB_USER'),
              'PASSWORD': config('DB_PASSWORD'),
              'HOST': config('DB_HOST'),
          }
      }
    (optional, Ref: same section: add an "OPTIONS": {"pool": True} block
    to use Django 5.1's connection pooling)
[ ] .env — add:
      DB_NAME=bookshelf
      DB_USER=bookshelf
      DB_PASSWORD=xxxxx
      DB_HOST=localhost
[ ] `python manage.py migrate` against the new, empty Postgres database —
    confirm every app's migrations apply cleanly (admin, auth, catalog,
    contenttypes, sessions, sites, taggit)
[ ] Load the data back in (Ref: "Loading the data into the new
    database"):
      
[ ] `python manage.py runserver`, confirm /admin/catalog/book/ shows all
    your original books

Deliverable: running Postgres container, psycopg installed,
bookshelf_data.json fixture, updated settings.py + .env, data reloaded
Definition of done: the project runs against Postgres with all
previously-created books, reviews, and tags intact.

============================================================
PHASE 21 — FULL-TEXT SEARCH ENGINE
python manage.py loaddata bookshelf_data.json============================================================
Book ref: "Adding full-text search to the blog", "Simple search lookups",
          "Searching against multiple fields", "Building a search view",
          "Stemming and ranking results", "Stemming and removing stop
          words in different languages", "Weighting queries", "Searching
          with trigram similarity"

Tasks:
[ ] bookshelf/settings.py — add 'django.contrib.postgres' to
    INSTALLED_APPS
[ ] Shell-test simple single-field search (Ref: "Simple search lookups"):
      python manage.py shell
      >>> from catalog.models import Book
      >>> Book.objects.filter(title__search='dune')
[ ] Shell-test multi-field search (Ref: "Searching against multiple
    fields"):
      >>> from django.contrib.postgres.search import SearchVector
      >>> Book.objects.annotate(
      ...     search=SearchVector('title', 'synopsis'),
      ... ).filter(search='dune')
[ ] catalog/forms.py:
      class SearchForm(forms.Form):
          query = forms.CharField()
[ ] catalog/views.py — book_search view (Ref: "Building a search view"):
      from django.contrib.postgres.search import SearchVector
      from .forms import SearchForm
      def book_search(request):
          form = SearchForm()
          query = None
          results = []
          if 'query' in request.GET:
              form = SearchForm(request.GET)
              if form.is_valid():
                  query = form.cleaned_data['query']
                  results = (
                      Book.published.annotate(
                          search=SearchVector('title', 'synopsis'),
                      )
                      .filter(search=query)
                  )
          return render(request, 'catalog/book/search.html',
                        {'form': form, 'query': query, 'results': results})
[ ] templates/catalog/book/search.html (mirrors search.html):
      {% extends "catalog/base.html" %}
      {% load catalog_tags %}
      {% block title %}Search{% endblock %}
      {% block content %}
        {% if query %}
          <h1>Books matching "{{ query }}"</h1>
          <h3>
            {% with results.count as total_results %}
              Found {{ total_results }} result{{ total_results|pluralize }}
            {% endwith %}
          </h3>
          {% for book in results %}
            <h4><a href="{{ book.get_absolute_url }}">{{ book.title }}</a></h4>
            {{ book.synopsis|markdown|truncatewords_html:12 }}
          {% empty %}
            <p>There are no results for your query.</p>
          {% endfor %}
          <p><a href="{% url 'catalog:book_search' %}">Search again</a></p>
        {% else %}
          <h1>Search for books</h1>
          <form method="get">
            {{ form.as_p }}
            <input type="submit" value="Search">
          </form>
        {% endif %}
      {% endblock %}
[ ] catalog/urls.py: path('search/', views.book_search, name='book_search')
[ ] Add stemming + ranking (Ref: "Stemming and ranking results"):
      from django.contrib.postgres.search import (
          SearchVector, SearchQuery, SearchRank
      )
      search_vector = SearchVector('title', 'synopsis')
      search_query = SearchQuery(query)
      results = (
          Book.published.annotate(
              search=search_vector,
              rank=SearchRank(search_vector, search_query)
          )
          .filter(search=search_query)
          .order_by('-rank')
      )
[ ] Optional — non-English stemming (Ref: "Stemming and removing stop
    words in different languages"): pass config='spanish' (or another
    language) to SearchVector and SearchQuery if your catalog includes
    non-English titles/synopses
[ ] Add weighting so title matches outrank synopsis matches (Ref:
    "Weighting queries"):
      search_vector = (
          SearchVector('title', weight='A')
          + SearchVector('synopsis', weight='B')
      )
      search_query = SearchQuery(query)
      results = (
          Book.published.annotate(
              search=search_vector,
              rank=SearchRank(search_vector, search_query)
          )
          .filter(rank__gte=0.3)
          .order_by('-rank')
      )
[ ] Trigram similarity (Ref: "Searching with trigram similarity"):
      python manage.py makemigrations --name=trigram_ext --empty catalog
    Edit the generated catalog/migrations/0005_trigram_ext.py:
      from django.contrib.postgres.operations import TrigramExtension
      from django.db import migrations
      class Migration(migrations.Migration):
          dependencies = [('catalog', '0004_book_tags')]
          operations = [TrigramExtension()]
      python manage.py migrate catalog
[ ] Update book_search to use TrigramSimilarity for typo-tolerant title
    search:
      from django.contrib.postgres.search import TrigramSimilarity
      results = (
          Book.published.annotate(
              similarity=TrigramSimilarity('title', query),
          )
          .filter(similarity__gt=0.1)
          .order_by('-similarity')
      )

Deliverable: SearchForm, book_search view (progressively upgraded through
plain search -> vector -> ranked -> weighted -> trigram), search.html,
0005_trigram_ext.py migration
Definition of done: /catalog/search/?query=dune returns ranked results by
relevance; a misspelled query (e.g. "dnue") still returns "Dune" once
trigram similarity is wired in.

------------------------------------------------------------
PHASE 22 — OPTIONAL / STRETCH: AI-assisted extension
------------------------------------------------------------
Book ref: "Expanding your project using AI"

Task:
[ ] The book's closing challenge is to use an LLM to extend the sitemap
    so tag-filtered list pages are included too (improves SEO beyond just
    individual post pages). Adapted for BookShelf CMS: extend BookSitemap
    (or add a second Sitemap subclass) so that tag pages like
    /catalog/tag/sci-fi/ are also listed in sitemap.xml, alongside the
    per-book URLs from Phase 18.

Deliverable: an extended (or additional) Sitemap class covering tag pages
Definition of done: /sitemap.xml includes both book detail URLs and
tag-filtered list URLs.

============================================================
MANAGEMENT COMMAND CHEAT SHEET — CHAPTER 3 ADDITIONS
============================================================
python -m pip install django-taggit==5.0.1
python -m pip install markdown==3.6
python -m pip install psycopg==3.1.18
python manage.py makemigrations catalog        # 0004_book_tags
python manage.py migrate
docker pull postgres:16.2
docker run --name=bookshelf_db -e POSTGRES_DB=bookshelf \
  -e POSTGRES_USER=bookshelf -e POSTGRES_PASSWORD=xxxxx \
  -p 5432:5432 -d postgres:16.2
python manage.py dumpdata --indent=2 --output=bookshelf_data.json
python manage.py loaddata bookshelf_data.json
python manage.py makemigrations --name=trigram_ext --empty catalog
python manage.py migrate catalog

============================================================
DOMAIN MAPPING TABLE — CHAPTER 3 ADDITIONS
============================================================
Post.tags (TaggableManager)         -> Book.tags (unchanged)
post_tags_ids                        -> book_tags_ids
similar_posts                         -> similar_books
blog_tags.py                           -> catalog_tags.py
total_posts                             -> total_books
show_latest_posts / latest_posts.html    -> show_latest_books / latest_books.html
get_most_commented_posts                  -> get_most_reviewed_books
markdown filter (post.body)                -> markdown filter (book.synopsis)
PostSitemap                                 -> BookSitemap
LatestPostsFeed / post_feed                  -> LatestBooksFeed / book_feed
SearchForm.query                              -> unchanged
post_search / blog:post_search                 -> book_search / catalog:book_search
blog/post/search.html                            -> catalog/book/search.html
blog_db / DB user "blog"                          -> bookshelf_db / DB user "bookshelf"
mysite_data.json                                   -> bookshelf_data.json
0005_trigram_ext.py deps on blog.0004_post_tags      -> deps on catalog.0004_book_tags

============================================================
OUT OF SCOPE (deliberately excluded — covered in Chapter 4)
============================================================
- User registration and login/logout views
- Django's authentication framework (auth views, password reset)
- Custom user profiles
- Social authentication (Google/GitHub login, etc.)

END OF ROADMAP

BOOKSHELF CMS — CHAPTER 4 IMPLEMENTATION ROADMAP
Mirrors: "Django 5 by Example", Chapter 4 — Building a Social Website
Prerequisite: Chapters 1-3 roadmaps complete (bookshelf project, catalog
app: Book, Review, tags, search, sitemap, feed all working).

FORMAT NOTE FOR THIS FILE: no implementation code is included below — no
class bodies, no field declarations written as code, no HTML/view/form
snippets. Every task describes WHAT to build (names, field types,
behavior, which template goes where) so you can write the actual Django
code yourself. Shell/manage.py commands are still spelled out, since
running a command isn't the part you're practicing.

PROJECT-STRUCTURE NOTE: the book restarts entirely here — new venv, new
project called "bookmarks" — because it's pivoting to a separate
image-sharing project for the rest of the book. This roadmap instead
folds Chapter 4's authentication system into your existing bookshelf
project as a new app, so your catalog work and reader-account work live
together. Say the word if you'd rather spin it out as a second project
the way the book does.

Scope rule: every task below maps 1:1 to a section in Chapter 4. Nothing
from Chapter 5+ (messages framework, email-based auth, Google social
auth, HTTPS via django-extensions, auto-creating profiles via signals) is
included on purpose — see OUT OF SCOPE at the end.

============================================================
0. NEW NAMING FOR THIS CHAPTER
============================================================
account app                    -> accounts app
Profile model                   -> ReaderProfile model (deliberate rename —
                                    same kind of domain-fit choice as
                                    Comment -> Review in Chapter 2; this is
                                    the profile of a reader of your catalog)
ProfileEditForm                  -> ReaderProfileEditForm
photo upload path
  users/%Y/%m/%d/                 -> readers/%Y/%m/%d/
Everything else (LoginForm, UserRegistrationForm, UserEditForm, the view
names login/logout/register/edit/dashboard, all the built-in auth view
names) is generic account-system vocabulary with no book-specific
concept to shift, so it's kept exactly as in the book.

Target end-state directory tree additions:

bookshelf/
    bookshelf/
        settings.py          (+ accounts app ordered before django.contrib.admin,
                                LOGIN_REDIRECT_URL / LOGIN_URL / LOGOUT_URL,
                                MEDIA_URL / MEDIA_ROOT, EMAIL_BACKEND for
                                local testing)
        urls.py                (+ accounts/ include, + static() media serving
                                  under DEBUG)
    accounts/
        admin.py                (ReaderProfileAdmin)
        forms.py                 (LoginForm, UserRegistrationForm,
                                   UserEditForm, ReaderProfileEditForm)
        models.py                 (ReaderProfile)
        views.py                  (user_login [temporary], dashboard,
                                    register, edit)
        urls.py                    (built up in stages across Phases 24-28,
                                    ending as a single include of
                                    django.contrib.auth.urls plus your own
                                    dashboard/register/edit paths)
        migrations/
            0001_initial.py          (ReaderProfile)
        templates/
            base.html
            registration/
                login.html
                logged_out.html
                password_change_form.html
                password_change_done.html
                password_reset_form.html
                password_reset_email.html
                password_reset_done.html
                password_reset_confirm.html
                password_reset_complete.html
            accounts/
                dashboard.html
                register.html
                register_done.html
                edit.html

============================================================
PHASE 23 — READER ACCOUNTS APP BOOTSTRAP
============================================================
Book ref: "Creating a social website project", "Starting the social
          website project"

Tasks:
[ ] Create a new app named `accounts` inside the bookshelf project
[ ] Register it in INSTALLED_APPS — the book specifically puts its
    account app FIRST in the list, ahead of django.contrib.admin, so that
    Django's template loader finds your custom auth templates (login,
    logged_out, etc.) before it finds admin's built-in versions of the
    same template names. Do the same here: put accounts ahead of
    django.contrib.admin.
[ ] Run the standard migrate to confirm the project still starts cleanly
    with the new (currently empty) app installed

Deliverable: accounts app scaffold, correctly ordered INSTALLED_APPS
Definition of done: manage.py check passes; accounts appears before
django.contrib.admin in INSTALLED_APPS.

============================================================
PHASE 24 — CUSTOM LOGIN VIEW (learning exercise, replaced in Phase 25)
============================================================
Book ref: "Using the Django authentication framework", "Creating a login
          view"

Tasks:
[ ] Build a LoginForm with two fields: a username field and a password
    field rendered with a password-style widget (so the browser masks
    input)
[ ] Build a user_login view with this exact branching behavior:
      - GET: show a blank LoginForm
      - POST: validate the form; call Django's authenticate() with the
        submitted credentials
      - if authenticate() returns no user: show one distinct response
        for "invalid login"
      - if it returns a user but is_active is False: a different
        response for "disabled account"
      - if it returns an active user: call login() to start the session,
        then a third distinct response for "authenticated successfully"
    (the point of building three distinct outcomes, per the book, is to
    see authenticate() and login() as two separate steps before you hand
    this job to Django's built-in view in the next phase)
[ ] Wire a URL for it, and create a minimal base template plus a login
    template that renders the form
[ ] Create a superuser and at least one ordinary reader account via
    /admin/ to have something to log in with

Deliverable: a working, disposable login view exercising
authenticate()/login() directly
Definition of done: you can trigger all three outcomes (bad credentials /
inactive account / success) by controlling what you submit and which
test account's is_active flag you toggle in /admin/.

============================================================
PHASE 25 — BUILT-IN LOGIN/LOGOUT VIEWS + DASHBOARD
============================================================
Book ref: "Using Django's built-in authentication views", "Login and
          logout views"

Tasks:
[ ] Retire the Phase 24 URL and swap in Django's built-in login view,
    plus a URL for the built-in logout view
[ ] Create the two templates Django's auth views expect by default,
    under a `registration/` template directory: one for the login page
    and one for the "logged out" confirmation page
      - the login template needs: an errors branch (Django's own
        AuthenticationForm raises a validation error on bad credentials,
        so check for form errors rather than writing your own check), a
        hidden "next" field so post-login redirects work, and the form
        itself
[ ] Build a dashboard view, protected with the login-required decorator,
    that renders a simple welcome page; wire its URL
[ ] In settings, point Django at your URL names for: where to send users
    after a successful login with no "next" param, where to send
    unauthenticated users who hit a protected view, and where "logout"
    points
[ ] Update the base template to branch on whether the current user is
    authenticated: show a nav menu + a greeting (falling back to
    username if no first name is set) + a logout control that POSTs
    (Django's logout view requires POST) when logged in; show a plain
    login link otherwise

Deliverable: full login/logout/dashboard loop using Django's built-in
views instead of your Phase 24 exercise
Definition of done: logging in lands you on the dashboard; logging out
lands you on the logged-out page; the header visibly changes based on
auth state; hitting the dashboard while logged out redirects you to log
in first and then back to the dashboard afterward.

============================================================
PHASE 26 — CHANGE PASSWORD VIEWS
============================================================
Book ref: "Change password views"

Tasks:
[ ] Add URLs for Django's built-in change-password view and its
    "done" confirmation view
[ ] Create the two matching templates under `registration/`: the change
    form, and a short success message page

Deliverable: working change-password flow
Definition of done: a logged-in reader can change their password and see
a confirmation page, then log out and back in with the new password.

============================================================
PHASE 27 — RESET PASSWORD VIEWS + CONSOLIDATING AUTH URLS
============================================================
Book ref: "Reset password views"

Tasks:
[ ] Add URLs for the four built-in reset-password views: the request
    form, the "email sent" confirmation, the confirm/set-new-password
    view (this one takes two URL parameters carried in the reset link),
    and the final "complete" success page
[ ] Create the five matching templates under `registration/`:
      - the reset-request form
      - the reset email itself — note this one is NOT extended from your
        base template, it's plain text meant to be emailed, and needs to
        include the generated reset link and the user's username
      - the "email sent" confirmation page
      - the confirm page — this one branches on whether the link is
        still valid; only show the new-password form if it is
      - the final success page
[ ] Add a "forgotten your password?" link to the login template
[ ] For local testing without a real mailbox, point EMAIL_BACKEND at
    Django's console backend so reset emails print to your terminal
    (or reuse the Gmail SMTP config you already set up in the Chapter 2
    roadmap if you'd rather see a real email)
[ ] Consolidation step (mirrors the book exactly): once every piece above
    works individually, retire every manual URL pattern you wrote across
    Phases 25-27 for login/logout/password-change/password-reset, and
    replace all of them with a single include of Django's built-in auth
    URL module. Confirm every URL name you were relying on (login,
    logout, password_change, password_change_done, password_reset,
    password_reset_done, password_reset_confirm,
    password_reset_complete) still resolves correctly even though you no
    longer declare any of those paths yourself.

Deliverable: full password-reset flow; auth URLs consolidated down to one
include line plus your own dashboard/register/edit paths
Definition of done: you can request a reset email, follow the link,
successfully set a new password, and log in with it — and your urls.py
no longer has individual path() entries for any of the built-in auth
views.

============================================================
PHASE 28 — READER REGISTRATION
============================================================
Book ref: "User registration and user profiles", "User registration"

Tasks:
[ ] Build a UserRegistrationForm as a model form built against the
    *dynamically retrieved* user model (not a hardcoded import of the
    User model — this keeps things swappable if you ever move to a
    custom user model later), including username, first name, and email,
    plus two extra plain (non-model) fields for a password and a
    repeated-password confirmation
[ ] Add a field-level validator on the repeated-password field that
    raises a validation error if it doesn't match the first password
    field
[ ] Build a register view: on a valid POST, build the user object
    without committing it to the database yet, set the password properly
    using the model's password-hashing method (never assign the raw
    password to the password field directly), then save; render a
    distinct "registration done" template afterward
[ ] Wire the URL, and build the two templates: the registration form
    page and the "welcome, you're registered" page
[ ] Add a "register here" link to the login template

Deliverable: working self-service registration flow
Definition of done: a new visitor can create an account (with a
correctly-rejected mismatched-password case along the way), and lands on
a "you're registered, now log in" page.

============================================================
PHASE 29 — READER PROFILE MODEL & MEDIA UPLOADS
============================================================
Book ref: "Extending the user model", "Installing Pillow and serving
          media files", "Creating migrations for the profile model"

Tasks:
[ ] Add a ReaderProfile model with: a one-to-one link to the user model
    (with cascading delete, so a profile disappears if its user is
    deleted), an optional date-of-birth date field, and an optional photo
    image field uploading into a dated `readers/` subdirectory (mirrors
    the book's `users/%Y/%m/%d/` path, renamed to fit the domain)
[ ] Install Pillow (Django's ImageField needs it to validate/process
    images)
[ ] Add the media URL prefix and the local media root path to settings,
    and wire the project urls.py to serve media files locally whenever
    DEBUG is on
[ ] Generate and apply the migration for ReaderProfile
[ ] Register ReaderProfile in the admin with a sensible list display
    (user, date of birth, photo) and a raw-ID widget for the user field
    (same reasoning as Book.added_by back in Chapter 1 — nicer than a
    giant dropdown once you have many users)
[ ] Update the register view so that a matching ReaderProfile row gets
    created automatically whenever a new user registers through the
    form. Note, same as the book: accounts created directly through
    /admin/ won't get this for free — that gap gets closed with Django
    signals in a later chapter, not this one.

Deliverable: ReaderProfile model + migration, admin registration, media
serving configured, auto-created on self-registration
Definition of done: registering a new reader through your Phase 28 form
creates both a User row and a matching ReaderProfile row; you can attach
a photo to a profile through /admin/ and retrieve it at the configured
media URL.

============================================================
PHASE 30 — PROFILE EDITING
============================================================
Book ref: continuation of "Extending the user model" (the edit view,
          templates, and dashboard links)

Tasks:
[ ] Build a UserEditForm covering first name, last name, and email on
    the built-in user model
[ ] Build a ReaderProfileEditForm covering date of birth and photo on
    your ReaderProfile model
[ ] Build a login-required edit view that instantiates both forms bound
    to the current user and their profile, validates both together on
    POST, and saves both only if both are valid
[ ] Build the edit template: a single form (remember it needs
    multipart encoding, since one of the two sub-forms includes a file
    upload) rendering both sub-forms with one submit button
[ ] Wire the URL, and add two links from the dashboard: one to this edit
    page, one to the change-password page

Deliverable: working profile-edit flow
Definition of done: a logged-in reader can update their name/email and
upload or replace their profile photo in a single submission; the
dashboard links to both this page and password-change.

------------------------------------------------------------
PHASE 31 — CONCEPTUAL ONLY: custom user model (no implementation)
------------------------------------------------------------
Book ref: "Using a custom user model"

This section of the book is a pointer, not something it actually builds:
Django lets you substitute the entire user model (by subclassing
AbstractUser) instead of bolting a profile onto the default one, trading
more flexibility for rougher integration with third-party packages that
assume the stock auth User. Nothing to build here — just worth knowing
this door exists if ReaderProfile ever starts to feel like it's straining
against the built-in User model's fixed fields.

============================================================
MANAGEMENT COMMAND CHEAT SHEET — CHAPTER 4 ADDITIONS
============================================================
python manage.py startapp accounts
python -m pip install Pillow==11.0.0
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser        # if you don't already have one

============================================================
DOMAIN MAPPING TABLE — CHAPTER 4 ADDITIONS
============================================================
account app                          -> accounts app
Profile model                         -> ReaderProfile model
ProfileEditForm                        -> ReaderProfileEditForm
users/%Y/%m/%d/ upload path              -> readers/%Y/%m/%d/
bookmarks (book's fresh restart project)  -> folded into the existing
                                              bookshelf project instead
                                              (flag if you'd rather split
                                              it out)

============================================================
OUT OF SCOPE (deliberately excluded — covered in Chapter 5)
============================================================
- Django's messages framework for user feedback
- Authenticating with email address instead of username
- Social authentication (Google login)
- Serving the dev server over HTTPS via django-extensions
- Auto-creating ReaderProfile rows via signals (closes the /admin/-created-
  user gap noted in Phase 29)

END OF ROADMAP
BOOKSHELF CMS — CHAPTER 5 IMPLEMENTATION ROADMAP
Mirrors: "Django 5 by Example", Chapter 5 — Implementing Social
         Authentication
Prerequisite: Chapters 1-4 roadmaps complete (bookshelf project, catalog
app fully built, accounts app with login/logout/password
change+reset/registration/ReaderProfile all working).

FORMAT NOTE (carried over from Chapter 4): no implementation code below —
no class bodies, no field/setting values written as literal code, no
HTML/view/form/pipeline snippets. Every task describes WHAT to build and
WHERE, so you write the actual Django code yourself. Shell commands and
external console steps (Google Cloud setup, hosts file edits) are still
spelled out, since those aren't the part you're practicing.

Scope rule: every task below maps 1:1 to a section in Chapter 5. Nothing
from Chapter 6+ (image bookmarking, many-to-many relationships, custom
form field behavior, image thumbnails, AJAX with JavaScript) is included
on purpose — see OUT OF SCOPE at the end.

============================================================
0. NEW NAMING FOR THIS CHAPTER
============================================================
account/authentication.py       -> accounts/authentication.py (path change
                                    only, following the app rename from
                                    Chapter 4)
create_profile (pipeline func)   -> create_reader_profile (renamed to
                                    match the Chapter 4 Profile ->
                                    ReaderProfile rename)
EmailAuthBackend                  -> unchanged — generic auth-system
                                     vocabulary, nothing domain-specific to
                                     shift (same reasoning as LoginForm,
                                     UserEditForm etc. in Chapter 4)
mysite.com (book's local dev host)  -> your choice — consider
                                       bookshelfcms.test instead of a
                                       .com-style name; .test is a
                                       reserved TLD (RFC 2606) meant
                                       exactly for this, so it can never
                                       collide with a real domain later
"Bookmarks" (Google Cloud project     -> "BookShelf CMS"
 name in the book)

Target end-state additions (on top of the Chapter 1-4 tree):

bookshelf/
    .env                       (+ GOOGLE_OAUTH2_KEY, GOOGLE_OAUTH2_SECRET)
    cert.crt                   (generated locally by runserver_plus —
                                 gitignore this)
    bookshelf/
        settings.py            (+ AUTHENTICATION_BACKENDS, ALLOWED_HOSTS,
                                  social_django + django_extensions in
                                  INSTALLED_APPS, SOCIAL_AUTH_GOOGLE_OAUTH2_KEY
                                  / _SECRET, SOCIAL_AUTH_PIPELINE)
        urls.py                 (+ social_django URL include, namespaced)
    accounts/
        authentication.py        (new: EmailAuthBackend,
                                    create_reader_profile)
        forms.py                  (+ clean_email validators on
                                     UserRegistrationForm and UserEditForm)
        templates/
            base.html               (+ messages display block)
            registration/
                login.html            (+ "Sign in with Google" link)

============================================================
PHASE 32 — MESSAGES FRAMEWORK FEEDBACK
============================================================
Book ref: "Using the messages framework"

Tasks:
[ ] Confirm django.contrib.messages and its middleware are already
    present — they're included by default ever since Chapter 1's
    startproject, so there's nothing new to install or register here
[ ] Update accounts/templates/base.html to render any queued messages:
    loop over the messages variable that the messages context processor
    already adds to every request's context, and for each one render its
    text plus its "tag" (info/success/warning/error/debug) as a CSS class
    so each severity can be styled distinctly; include a way to dismiss
    each one
[ ] Update your Chapter 4 Phase 30 profile-edit view to queue a success
    message once both the user-edit and profile-edit forms save cleanly,
    and an error message if either form fails validation

Deliverable: message rendering wired into base.html, success/error
messages wired into the edit view
Definition of done: successfully saving your profile shows a dismissible
success banner; submitting something invalid (try a malformed date of
birth) shows a dismissible error banner instead.

============================================================
PHASE 33 — CUSTOM EMAIL AUTHENTICATION BACKEND
============================================================
Book ref: "Building a custom authentication backend"

Tasks:
[ ] Create a new accounts/authentication.py module
[ ] Define an EmailAuthBackend class implementing the two methods
    Django's auth system requires from any backend:
      - one that accepts a request plus username/password-shaped
        arguments (keep the parameter named `username` even though
        you'll treat it as an email address — that's what lets this
        backend slot into the existing login form and built-in views
        without any changes on their side), looks up the matching user
        by email, and returns that user only if the password checks out
        against it. Treat both "no user with that email" and "more than
        one user with that email" as an ordinary failed login, not an
        exception that bubbles up.
      - one that accepts a user ID and returns the matching user, used
        by Django to reload the authenticated user for the rest of the
        session
[ ] Add this backend's dotted path to AUTHENTICATION_BACKENDS, after the
    default ModelBackend — order matters: Django tries backends in
    order and stops at the first success, so username/password logins
    should keep resolving via the default backend exactly as before,
    with your new backend only coming into play for email-based logins

Deliverable: accounts/authentication.py with EmailAuthBackend, updated
AUTHENTICATION_BACKENDS setting
Definition of done: the same login form now accepts either a username or
an email address in the "username" field, with the same password, and
logs the reader in either way.

============================================================
PHASE 34 — PREVENTING DUPLICATE EMAIL REGISTRATION
============================================================
Book ref: "Preventing users from using an existing email address"

Tasks:
[ ] Add a field-level validator on UserRegistrationForm's email field
    that rejects the submission if any existing user already has that
    exact email address
[ ] Add the equivalent validator on UserEditForm's email field — but
    this one must exclude the current user from the "is this email
    already taken" check, or every reader would fail validation the
    moment they re-submit their own unchanged email

Deliverable: updated UserRegistrationForm and UserEditForm
Definition of done: registering with an email already in use is rejected
with a clear message; editing your profile without touching your email
still saves fine; trying to change your email to one that belongs to
someone else is rejected.

============================================================
PHASE 35 — SOCIAL AUTH PACKAGE BOOTSTRAP + LOCAL DOMAIN SETUP
============================================================
Book ref: "Adding social authentication to your site"

Tasks:
[ ] Install Python Social Auth's Django integration package
[ ] Add its app to INSTALLED_APPS and run migrate — it ships its own
    migrations to track linked social identities
[ ] Include its URL patterns in bookshelf's root urls.py under a
    namespace (the book uses 'social') and whatever URL prefix you like
[ ] Pick a fake local hostname for testing (see the naming note at the
    top — bookshelfcms.test is a solid choice) and add a line to your
    machine's hosts file (/etc/hosts on Linux/macOS, or
    C:\Windows\System32\Drivers\etc\hosts on Windows) pointing it at
    127.0.0.1
[ ] Add that hostname — plus localhost and 127.0.0.1 for good measure —
    to ALLOWED_HOSTS in settings.py
[ ] Confirm you hit Django's "invalid host header" error before making
    the ALLOWED_HOSTS change, and that it's gone afterward when loading
    your login page through the new hostname (still on plain HTTP at
    this stage)

Deliverable: social_django installed and migrated, its URLs included, a
local domain mapped in your hosts file and allowed in settings
Definition of done: your login page loads over http://<your-domain>:8000/
with no host-header error.

============================================================
PHASE 36 — HTTPS DEV SERVER VIA DJANGO EXTENSIONS
============================================================
Book ref: "Running the development server through HTTPS"

Tasks:
[ ] Install Django Extensions, Werkzeug (its RunServerPlus command needs
    Werkzeug's debugger layer), and pyOpenSSL (needed for the TLS
    support)
[ ] Add django_extensions to INSTALLED_APPS
[ ] Run the project with the runserver_plus management command instead
    of the ordinary runserver, pointing it at a certificate filename so
    it generates a self-signed certificate for you on the fly
[ ] Load your local domain over https:// and accept your browser's
    self-signed-certificate warning (the exact click-path differs by
    browser, but you're looking for the "proceed anyway" equivalent)

Deliverable: dev server reachable over HTTPS on your local domain
Definition of done: your login page loads over https://<your-domain>:8000/,
with your browser showing the connection as secure (even if flagged as
self-signed).

============================================================
PHASE 37 — GOOGLE OAUTH2 SIGN-IN
============================================================
Book ref: "Authentication using Google"

Tasks:
[ ] Add Python Social Auth's Google OAuth2 backend's dotted path to
    AUTHENTICATION_BACKENDS, after your email backend
[ ] In Google Cloud Console: create a new project named after BookShelf
    CMS (not "Bookmarks"), configure its OAuth consent screen as an
    External app, add your own Google account as a test user, then
    create an OAuth client ID of type "Web application" — set its
    authorized JavaScript origin and authorized redirect URI to your
    HTTPS local domain from Phases 35-36 (the redirect URI needs the
    specific completion path Python Social Auth's Google backend expects
    — check its docs or the URL namespace you registered in Phase 35 for
    the exact shape)
[ ] Store the resulting client ID and secret in your .env file, not
    directly in settings.py
[ ] Install python-decouple if this is a fresh environment that doesn't
    already have it (you used it for email and database credentials back
    in the Chapter 2/3 roadmaps), and load the two new values into two
    new SOCIAL_AUTH_GOOGLE_OAUTH2_* settings
[ ] Add a "Sign in with Google" link to your login template, pointing at
    the social-auth "begin" URL for the google-oauth2 backend name

Deliverable: Google OAuth2 backend configured, credentials in .env, a
sign-in link on the login page
Definition of done: clicking "Sign in with Google" on your HTTPS local
domain walks you through Google's consent screen and lands you back on
your dashboard, logged in.

============================================================
PHASE 38 — AUTO-CREATING READER PROFILES FOR SOCIAL SIGN-INS
============================================================
Book ref: "Creating a profile for users that register with social
          authentication"

Tasks:
[ ] Add a SOCIAL_AUTH_PIPELINE setting to settings.py, starting from
    Python Social Auth's documented default pipeline (look it up rather
    than retyping it from memory — it's a specific ordered list of
    library-provided function paths)
[ ] Write a small pipeline function in accounts/authentication.py —
    name it create_reader_profile to match the Chapter 4 rename — that
    accepts the backend and user arguments every pipeline step receives,
    and uses get_or_create() against your ReaderProfile model so it's a
    no-op for a returning reader and creates exactly one profile for a
    brand-new one
[ ] Insert your new step into the SOCIAL_AUTH_PIPELINE list immediately
    after the step that creates new User objects, so a User is
    guaranteed to already exist by the time your function runs
[ ] Test it: delete (in /admin/) a user you previously created via
    Google sign-in, sign in with Google again, and confirm a new User
    and a new ReaderProfile both appear

Deliverable: SOCIAL_AUTH_PIPELINE setting, create_reader_profile pipeline
function
Definition of done: every reader created via Google sign-in ends up with
a matching ReaderProfile automatically. Note: users created directly
through /admin/ still won't get one — that gap is closed with Django
signals in Chapter 7, not here.

============================================================
MANAGEMENT COMMAND CHEAT SHEET — CHAPTER 5 ADDITIONS
============================================================
python -m pip install social-auth-app-django==5.4.0
python -m pip install django-extensions==3.2.3
python -m pip install werkzeug==3.0.2
python -m pip install pyOpenSSL==24.1.0
python -m pip install python-decouple==3.8    # if not already installed
python manage.py migrate                       # after adding social_django
python manage.py runserver_plus --cert-file cert.crt

============================================================
DOMAIN MAPPING TABLE — CHAPTER 5 ADDITIONS
============================================================
account/authentication.py       -> accounts/authentication.py
create_profile                   -> create_reader_profile
Profile.objects.get_or_create()   -> ReaderProfile.objects.get_or_create()
mysite.com                         -> bookshelfcms.test (suggested)
"Bookmarks" Google Cloud project    -> "BookShelf CMS"

============================================================
OUT OF SCOPE (deliberately excluded — covered in Chapter 6)
============================================================
- The image-bookmarking system itself
- Many-to-many relationships
- Customizing form field behavior
- Generating image thumbnails
- AJAX functionality with JavaScript

END OF ROADMAP